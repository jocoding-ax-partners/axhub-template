# axhub 배포 설정
# 이 앱은 Dockerfile 로 빌드돼요 (astro build, @astrojs/node standalone → node dist/server/entry.mjs).
# apphub.yaml 은 컨테이너 포트·헬스체크 override 일 뿐이에요. (Dockerfile 의 EXPOSE 와 맞추세요.)
runtime:
  port: 3000
  health_path: /
